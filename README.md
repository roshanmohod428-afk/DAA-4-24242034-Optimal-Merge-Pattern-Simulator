# DAA-4-24242034-Optimal-Merge-Pattern-Simulator
Create a program that takes multiple sorted files (or arrays) with different sizes and determines the minimum cost of merging them using the Greedy approach (like Huffman-style merging).
